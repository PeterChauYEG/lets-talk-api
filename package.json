{
  "name": "workspace",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "./node_modules/babel-cli/bin/babel-node.js --presets node6 index.js",
    "deploy": "gcloud app deploy",
    "allowPort": "gcloud compute firewall-rules create default-allow-websockets --allow tcp:80 --target-tags websocket --description 'Allow websocket traffic on port 80'"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-node6": "^11.0.0",
    "express": "^4.14.0",
    "socket.io": "^1.7.2"
  }
}
